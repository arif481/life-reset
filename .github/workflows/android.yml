name: Build Android APK

on:
  push:
    branches: [ main, feature/real-time-sync-analytics-settings ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Setup JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
    
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
    
    - name: Install dependencies
      run: npm ci || npm install
    
    - name: Build web assets
      run: npm run build
    
    - name: Sync Capacitor to Android
      run: npx cap sync android
    
    - name: Make gradlew executable
      run: chmod +x android/gradlew
    
    - name: Build Debug APK
      run: |
        cd android
        ./gradlew assembleDebug -Pandroid.injected.signing.store.file=../debug.keystore -Pandroid.injected.signing.store.password=android -Pandroid.injected.signing.key.alias=androiddebugkey -Pandroid.injected.signing.key.password=android --no-daemon
    
    - name: Get Debug Signing SHA-1 (for Firebase)
      run: |
        echo "=== SHA-1 Fingerprint for Firebase ==="
        keytool -list -v -keystore android/debug.keystore -alias androiddebugkey -storepass android -keypass android | grep "SHA1:" | awk '{print $2}'
        echo ""
        echo "=== IMPORTANT: Add this SHA-1 to Firebase Console ===" 
        echo "Go to: Firebase Console > Project Settings > Your Apps > Android > Add fingerprint"
    
    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: android/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30

  # Optional: Build release APK (requires signing setup)
  # release:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: github.ref == 'refs/heads/main'
  #   
  #   steps:
  #   - uses: actions/checkout@v4
  #   
  #   - name: Setup build environment
  #     # ... same as above
  #   
  #   - name: Build Release APK
  #     env:
  #       KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
  #       KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
  #       KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
  #     run: |
  #       cd android
  #       ./gradlew assembleRelease
  #   
  #   - name: Upload Release APK
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: app-release
  #       path: android/app/build/outputs/apk/release/app-release.apk
